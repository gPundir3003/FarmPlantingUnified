name: farmapp
description: A new Flutter project.

publish_to: 'none' # Remove this line if you're publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  http: ^1.1.0
  image_picker: ^0.8.7+3
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^8.0.0  # 🔐 For storing JWT tokens securely

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/plot_map.jpg
    - assets/crops/
    - assets/farm.jpg
