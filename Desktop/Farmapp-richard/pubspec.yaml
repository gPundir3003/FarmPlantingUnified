name: flutter_application_1
description: A new Flutter project.

publish_to: 'none' # Remove this line if you plan to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.0.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/plot_map.jpg
    - assets/crops/orange.png
    - assets/crops/wheat.png
    - assets/crops/leafy.png
    - assets/crops/tree.png
    - assets/crops/lettuce.png
    - assets/logo.png
   

